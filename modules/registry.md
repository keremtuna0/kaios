# Module Registry Compatibility Note

The authoritative Module Registry contract is
[module-registry.md](module-registry.md). This retained file exists only for
older references and does not define a second module or separate behavior.
